{
  "name": "CTF Base Devcontainer",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postCreateCommand": "sudo apt update && sudo apt install -y build-essential gdb python3-pip file && pip3 install pwntools"
}
